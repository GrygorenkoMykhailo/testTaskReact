//import { QuizType } from "../../types"
import { useParams } from "react-router"

export const Quiz = () => {
    const params = useParams();
    console.log(params);
    return (
        <>
            quiz)
        </>
    )
}